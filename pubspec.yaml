name: flutter_rest_api_networking
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  
  # Networking
  http: ^0.13.3

  # State Management
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linter Flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
